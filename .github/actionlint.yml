paths:
  .github/workflows/release.yml:
    ignore:
      # GitHub supports concurrency.queue, but actionlint v1.7.12 does not parse it yet.
      - 'unexpected key "queue" for "concurrency" section\. expected one of "cancel-in-progress", "group"'
